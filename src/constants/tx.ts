// Transaction-confirmation polling constants.

/** Overall timeout (ms) when polling for transaction confirmation. */
export const TX_POLL_TIMEOUT_MS = 60_000
/** Delay (ms) between confirmation-status polls. */
export const TX_POLL_INTERVAL_MS = 2_000
